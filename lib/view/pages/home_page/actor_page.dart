part of '../base.dart';

class Actorpage extends StatelessWidget {
  const Actorpage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('this is Actor page'),
    );
  }
}
